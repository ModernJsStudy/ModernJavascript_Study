# 원시 타입(primitive type) vs 객체 타입(object/reference type) 

// 원시 타입 값, 즉 원시 값은 변경 불가능한 값 = 읽기 전용 값, 변경 불가인 값
// 상수는 변수의 반대, 재할당이 금지된 값

// 객체 타입의 값, 즉 객체는 변경 가능한 값
원시 값을 변수에 할당하면 변수에는 실제 값이 저장된다.
객체를 변수에 할당하면 변수에는 참조 값이 저장된다.

참조 값이 복사되어 전달된다. 이를 참조에 의한 전달
